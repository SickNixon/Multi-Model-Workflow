{
  "$schema": "../node_modules/@tauri-apps/cli/schema/acl/capability.json",
  "identifier": "ai-panels",
  "description": "Permissions for AI panel WebViews — enables IPC back to the orchestrator via bridge_event",
  "windows": ["gemini", "deepseek", "grok", "claude"],
  "permissions": [
    "core:default",
    "core:event:allow-emit"
  ]
}
